meta {
  name: query
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/meters/query
  body: json
  auth: inherit
}

headers {
  x-tenant-slug: {{tenant_slug}}
}

body:json {
  {
    "meter_slug":"api_requests",
    "group_by": ["organization", "user", "method", "endpoint"]
  }
}
